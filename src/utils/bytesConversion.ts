import { ethers } from "ethers";

// string to bytes32
// export const toBytes32Conversion = (text: string) =>
//   ethers.utils.formatBytes32String(text);

// bytes 32 to string
// export const toStringConversion = (bytes32: string) =>
//   ethers.utils.parseBytes32String(bytes32);
